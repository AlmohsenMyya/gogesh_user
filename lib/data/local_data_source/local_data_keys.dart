enum LocalDataKeys {
  loggedIn,
  accessToken,
  userType,
  isDarkTheme,
  languageKey,
  currency,
  exchangeRate,
  privacyPolicy,
  profile,
  customerID,
  name,
  fcmToken,
  unreadMessages,
}

enum UserType { authenticated, gust, onboarding }
